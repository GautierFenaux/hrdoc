<form method="post" action="{{ path('app_cet_delete', {'id': cet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cet.id) }}">
    <button class="btn">Delete</button>
</form>
