<div class="side-nav" data-controller="sidenav">
	<div class="sidenav-menu-wrapper">
		<div class="sidenav-top">
			<div class="logo">
				<a href="{{ path('app_dashboard')}}"><img src="{{ asset('images/hrdoc_logo.png') }}" alt="logo matmut"></a>
			</div>
			<ul class="sidenav-menu">
				<li id="collaborator" class="menu-item-has-children" data-sidenav-target="collabMenu">
					<a href="{{ path('app_dashboard')}}" class="side-nav-focus">
						Tableau de bord</a>
					<ul class="slide"></ul>
				</li>
				{% if 'ROLE_MANAGER' in app.user.roles %}
					<li id="managerNav" class="menu-item-has-children" data-sidenav-target="managerMenu">
						<a>Manager</a>
						<ul class="slide">
							<li>
								<a href="{{ path('app_manager_dashboard')}}" class="side-nav-focus">
									<i class="fa-solid fa-table-columns"></i>
									Tableau de bord</a>
							</li>
						</ul>
					</li>
				{% endif %}
				{% if 'ROLE_ADMIN' in app.user.roles %}
					<li id="rhNav" class="menu-item-has-children" data-sidenav-target="rhMenu">
						<a>RH</a>
						<ul class="slide">
							<li>
								<a href="{{ path('app_rh_dashboard')}}" class="side-nav-focus">
									<i class="fa-solid fa-table-columns"></i>
									Tableau de bord</a>
							</li>
							<li>
								<a href="{{ path('admin')}}">
									<i class="fas fa-user-cog"></i>
									Administrer</a>
							</li>
						</ul>
					</li>
				{% endif %}
			</ul>
		</div>
		<div class="sidenav-profil-wrapper">
			<div class="profil-access">
				<ul>
					<a href="{{ path('app_profil')}}" class="side-nav-focus">
						<li>
							<i class="fas fa-user"></i>
							<span class="side-nav-focus">Profil</span>
						</li>
					</a>
					<a href="{{ path('app_logout')}}" class="side-nav-focus">
						<li>
							<i class="fas fa-sign-out-alt"></i>
							<span class="side-nav-focus">Déconnexion</span>
						</li>
					</a>
				</ul>
			</div>

			{% if app.user %}
				<li class="username-button" data-sidenav-target="profil">
					{{ app.user.name }}
					{{ app.user.surName }}
					<i class="fa-solid fa-chevron-down"></i>
				</li>
			{% endif %}

		</div>
	</div>
</div>