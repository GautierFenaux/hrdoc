<form method="post" action="{{ path('app_retour_sur_site_delete', {'id': retour_sur_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retour_sur_site.id) }}">
    <button class="btn">Delete</button>
</form>
